# Полный функционал администратора

## 📋 Обзор

Администратор имеет полный доступ ко всем функциям системы и может управлять абсолютно всем функционалом проекта.

---

## 👥 Управление пользователями

### Список пользователей
- **GET** `/api/admin/users`
  - Получить список всех пользователей
  - Фильтры: роль, поиск по email/имени
  - Пагинация

### Детали пользователя
- **GET** `/api/admin/users/[id]`
  - Полная информация о пользователе
  - Список enrollments
  - Статистика (прогресс, ДЗ, квизы, комментарии, сессии)

### Создание пользователя
- **POST** `/api/admin/users`
  - Создание нового пользователя
  - Установка роли, email, пароля
  - Audit log

### Обновление пользователя
- **PATCH** `/api/admin/users/[id]`
  - Изменение email, имени, роли
  - Смена пароля (инвалидирует все сессии)
  - Audit log

### Удаление пользователя
- **DELETE** `/api/admin/users/[id]`
  - Удаление пользователя (нельзя удалить себя)
  - Каскадное удаление всех связанных данных
  - Audit log

### Impersonation (Вход от имени пользователя)
- **POST** `/api/admin/users/[id]/impersonate`
  - Вход под другим пользователем
  - Сохранение оригинального токена администратора
  - Audit log + Security logging

### Управление enrollments
- **GET** `/api/admin/users/[id]/enrollments` - список enrollments
- **POST** `/api/admin/users/[id]/enrollments` - создать enrollment
- **DELETE** `/api/admin/users/[id]/enrollments` - удалить enrollment

### Управление прогрессом
- **GET** `/api/admin/users/[id]/progress` - просмотр прогресса
- **PATCH** `/api/admin/users/[id]/progress/update` - обновить прогресс
- **DELETE** `/api/admin/users/[id]/progress/update` - сбросить прогресс

### Управление сессиями
- **GET** `/api/admin/users/[id]/sessions` - все сессии пользователя
- **DELETE** `/api/admin/users/[id]/sessions?sessionId=xxx` - удалить конкретную сессию
- **DELETE** `/api/admin/users/[id]/sessions?all=true` - удалить все сессии

### Управление 2FA
- **GET** `/api/admin/users/[id]/2fa` - информация о 2FA
- **DELETE** `/api/admin/users/[id]/2fa` - отключить 2FA пользователя

---

## 📚 Управление курсами

### Список курсов
- **GET** `/api/admin/courses`
  - Все курсы с базовой информацией
  - Сортировка по дате создания

### Создание курса
- **POST** `/api/admin/courses`
  - Создание нового курса
  - Автоматическая генерация slug
  - Audit log

### Детали курса
- **GET** `/api/admin/courses/[id]`
  - Полная информация о курсе
  - Все модули и уроки
  - Сортировка по orderIndex

### Обновление курса
- **PATCH** `/api/admin/courses/[id]`
  - Изменение названия, описания, обложки
  - Публикация/скрытие курса
  - Audit log

### Удаление курса
- **DELETE** `/api/admin/courses/[id]`
  - Каскадное удаление модулей и уроков
  - Audit log

---

## 📦 Управление модулями

### Создание модуля
- **POST** `/api/admin/modules`
  - Создание модуля в курсе
  - Автоматическое назначение orderIndex
  - Транзакция для атомарности
  - Audit log

### Обновление модуля
- **PATCH** `/api/admin/modules/[id]`
  - Изменение названия
  - Audit log

### Удаление модуля
- **DELETE** `/api/admin/modules/[id]`
  - Каскадное удаление уроков
  - Audit log

### Изменение порядка модулей
- **POST** `/api/admin/modules/reorder`
  - Массовое изменение orderIndex
  - Транзакция для атомарности
  - Audit log

---

## 📖 Управление уроками

### Создание урока
- **POST** `/api/admin/lessons`
  - Создание урока в модуле
  - Типы: video, text, quiz
  - Автоматическое назначение orderIndex
  - Транзакция для атомарности
  - Audit log

### Детали урока
- **GET** `/api/admin/lessons/[id]`
  - Полная информация об уроке
  - Связь с модулем и курсом

### Обновление урока
- **PATCH** `/api/admin/lessons/[id]`
  - Все поля урока:
    - `title`, `type`, `content`
    - `videoId`, `videoDuration`, `thumbnailUrl`
    - `isFree`, `isStopLesson`
    - `dripRule` (JSON)
    - `settings` (JSON)
  - **Quiz settings:**
    - `quizMaxAttempts` (1-10)
    - `quizPassingScore` (0-100%)
    - `quizTimeLimit` (секунды)
    - `quizRequiresReview` (boolean)
  - Audit log

### Удаление урока
- **DELETE** `/api/admin/lessons/[id]`
  - Каскадное удаление прогресса и ДЗ
  - Audit log

### Изменение порядка уроков
- **POST** `/api/admin/lessons/reorder`
  - Массовое изменение orderIndex
  - Транзакция для атомарности
  - Audit log

### Настройки урока
- **PATCH** `/api/admin/lessons/[id]/settings`
  - `commentsEnabled` - включить/отключить комментарии
  - `homeworkDeadline` - жесткий дедлайн ДЗ
  - `homeworkSoftDeadline` - мягкий дедлайн ДЗ
  - `quizDeadline` - дедлайн квиза
  - Audit log

### Управление медиа урока
- **POST** `/api/admin/lessons/[id]/media` - прикрепить файл
- **DELETE** `/api/admin/lessons/[id]/media?mediaId=xxx` - открепить файл

---

## 📝 Управление домашними заданиями

### Список всех ДЗ
- **GET** `/api/admin/homework`
  - Все домашние задания в системе
  - Фильтры:
    - `status` (pending/approved/rejected)
    - `courseId` - по курсу
    - `userId` - по пользователю
    - `lessonId` - по уроку
  - Пагинация (limit, offset)
  - Количество версий в истории

### Детали ДЗ с историей
- **GET** `/api/admin/homework/[id]`
  - Полная информация о ДЗ
  - Все версии из истории
  - Информация о студенте, уроке, кураторе

### Удаление ДЗ
- **DELETE** `/api/admin/homework/[id]`
  - Физическое удаление ДЗ
  - Каскадное удаление истории
  - Audit log

---

## 💬 Управление комментариями

### Список комментариев
- **GET** `/api/admin/comments`
  - Все комментарии в системе
  - Фильтры:
    - `lessonId` - по уроку
    - `userId` - по пользователю
    - `includeDeleted` - включая удаленные
  - Пагинация
  - Количество ответов

### Удаление комментария
- **DELETE** `/api/admin/comments/[id]`
  - Физическое удаление (только админ)
  - Каскадное удаление ответов
  - Audit log

### Восстановление комментария
- **PATCH** `/api/admin/comments/[id]`
  - Восстановление удаленного комментария
  - Изменение статуса `isDeleted`
  - Audit log

---

## 🧩 Управление квизами

### Список попыток квизов
- **GET** `/api/admin/quiz/attempts`
  - Все попытки квизов
  - Фильтры:
    - `lessonId` - по уроку
    - `userId` - по пользователю
    - `isPassed` - пройденные/не пройденные
    - `requiresReview` - требующие проверки
  - Пагинация

### Детали попытки
- **GET** `/api/admin/quiz/attempts/[id]`
  - Полная информация о попытке
  - Ответы пользователя
  - Результаты проверки
  - Информация о кураторе

### Сброс попыток
- **DELETE** `/api/admin/quiz/attempts/[id]`
  - Сброс всех попыток пользователя по уроку
  - Сброс прогресса урока
  - Audit log

---

## 🔔 Управление уведомлениями

### Массовая отправка
- **POST** `/api/admin/notifications/send`
  - Отправка уведомлений пользователям
  - Параметры:
    - `userIds` - массив ID (если не указано - всем)
    - `type` - тип уведомления
    - `title` - заголовок
    - `message` - сообщение
    - `link` - ссылка (опционально)
  - Учитывает настройки пользователей
  - Audit log с количеством отправленных

---

## 👥 Управление группами

### Список групп
- **GET** `/api/admin/groups`
  - Все группы в системе

### Создание группы
- **POST** `/api/admin/groups`
  - Создание новой группы
  - Audit log

### Детали группы
- **GET** `/api/admin/groups/[id]`
  - Информация о группе
  - Список участников
  - Количество участников

### Обновление группы
- **PATCH** `/api/admin/groups/[id]`
  - Изменение названия, описания
  - Audit log

### Удаление группы
- **DELETE** `/api/admin/groups/[id]`
  - Удаление группы (только если нет участников)
  - Audit log

### Управление участниками
- **POST** `/api/admin/groups/[id]/members` - добавить участников
- **DELETE** `/api/admin/groups/[id]/members?userId=xxx` - удалить участника

### Массовое зачисление
- **POST** `/api/admin/groups/[id]/enrollments`
  - Зачисление всех участников группы на курс
  - Создание уведомлений для новых enrollments
  - Audit log

---

## 📂 Управление медиа-библиотекой

### Список файлов
- **GET** `/api/admin/media`
  - Все файлы в библиотеке
  - Фильтры: `mimeType`
  - Пагинация
  - Статистика (общий размер)

### Создание записи о файле
- **POST** `/api/admin/media`
  - Создание записи в библиотеке
  - Генерация signed URL

### Получение signed URL
- **GET** `/api/admin/media/[id]`
  - Получить signed URL для доступа к файлу
  - Автоматическое обновление истекших URL

### Удаление файла
- **DELETE** `/api/admin/media/[id]`
  - Удаление файла (только если не используется)
  - Audit log

---

## 📊 Аналитика и статистика

### Общая статистика
- **GET** `/api/admin/stats`
  - Базовая статистика системы

### Расширенная аналитика
- **GET** `/api/admin/analytics`
  - Общая статистика:
    - Количество студентов (всего, активных)
    - Количество курсов (всего, опубликованных)
    - Статистика по ДЗ (pending, approved, rejected)
    - Средний прогресс по курсам
  - Ежедневная активность
  - Воронка завершения курса (`?courseId=xxx`)
    - Процент завершения по модулям
    - Процент отсева

### Audit Logs
- **GET** `/api/admin/audit-logs`
  - История всех действий в системе
  - Последние 50 записей
  - Информация о пользователе, действии, сущности

---

## ⚙️ Дополнительные функции

### Восстановление из impersonation
- **POST** `/api/auth/impersonate/restore`
  - Восстановление оригинального аккаунта администратора
  - Автоматический редирект на `/admin`

### Проверка impersonation
- **GET** `/api/auth/impersonate/check`
  - Проверка активной сессии impersonation

---

## 🔒 Безопасность и Audit Logging

Все действия администратора логируются в `AuditLog`:
- `CREATE_*` - создание сущностей
- `UPDATE_*` - обновление сущностей
- `DELETE_*` - удаление сущностей
- `IMPERSONATE_USER` - вход от имени пользователя
- `RESTORE_FROM_IMPERSONATION` - возврат из impersonation
- `SEND_NOTIFICATIONS` - массовая отправка уведомлений
- `RESET_QUIZ_ATTEMPTS` - сброс попыток квиза
- `UPDATE_USER_PROGRESS` - изменение прогресса
- `DEACTIVATE_SESSION` - деактивация сессии
- `DISABLE_USER_2FA` - отключение 2FA

---

## 📋 Полный список API endpoints для администратора

### Пользователи
- `GET /api/admin/users` - список
- `POST /api/admin/users` - создать
- `GET /api/admin/users/[id]` - детали
- `PATCH /api/admin/users/[id]` - обновить
- `DELETE /api/admin/users/[id]` - удалить
- `POST /api/admin/users/[id]/impersonate` - вход от имени
- `GET /api/admin/users/[id]/enrollments` - enrollments
- `POST /api/admin/users/[id]/enrollments` - создать enrollment
- `DELETE /api/admin/users/[id]/enrollments` - удалить enrollment
- `GET /api/admin/users/[id]/progress` - прогресс
- `PATCH /api/admin/users/[id]/progress/update` - обновить прогресс
- `DELETE /api/admin/users/[id]/progress/update` - сбросить прогресс
- `GET /api/admin/users/[id]/sessions` - сессии
- `DELETE /api/admin/users/[id]/sessions` - удалить сессии
- `GET /api/admin/users/[id]/2fa` - информация о 2FA
- `DELETE /api/admin/users/[id]/2fa` - отключить 2FA

### Курсы
- `GET /api/admin/courses` - список
- `POST /api/admin/courses` - создать
- `GET /api/admin/courses/[id]` - детали
- `PATCH /api/admin/courses/[id]` - обновить
- `DELETE /api/admin/courses/[id]` - удалить

### Модули
- `POST /api/admin/modules` - создать
- `PATCH /api/admin/modules/[id]` - обновить
- `DELETE /api/admin/modules/[id]` - удалить
- `POST /api/admin/modules/reorder` - изменить порядок

### Уроки
- `POST /api/admin/lessons` - создать
- `GET /api/admin/lessons/[id]` - детали
- `PATCH /api/admin/lessons/[id]` - обновить
- `DELETE /api/admin/lessons/[id]` - удалить
- `POST /api/admin/lessons/reorder` - изменить порядок
- `PATCH /api/admin/lessons/[id]/settings` - настройки урока
- `POST /api/admin/lessons/[id]/media` - прикрепить медиа
- `DELETE /api/admin/lessons/[id]/media` - открепить медиа

### Домашние задания
- `GET /api/admin/homework` - список всех ДЗ
- `GET /api/admin/homework/[id]` - детали с историей
- `DELETE /api/admin/homework/[id]` - удалить ДЗ

### Комментарии
- `GET /api/admin/comments` - список всех комментариев
- `DELETE /api/admin/comments/[id]` - удалить комментарий
- `PATCH /api/admin/comments/[id]` - восстановить/изменить статус

### Квизы
- `GET /api/admin/quiz/attempts` - список попыток
- `GET /api/admin/quiz/attempts/[id]` - детали попытки
- `DELETE /api/admin/quiz/attempts/[id]` - сбросить попытки

### Уведомления
- `POST /api/admin/notifications/send` - массовая отправка

### Группы
- `GET /api/admin/groups` - список
- `POST /api/admin/groups` - создать
- `GET /api/admin/groups/[id]` - детали
- `PATCH /api/admin/groups/[id]` - обновить
- `DELETE /api/admin/groups/[id]` - удалить
- `POST /api/admin/groups/[id]/members` - добавить участников
- `DELETE /api/admin/groups/[id]/members` - удалить участника
- `POST /api/admin/groups/[id]/enrollments` - массовое зачисление

### Медиа
- `GET /api/admin/media` - список файлов
- `POST /api/admin/media` - создать запись
- `GET /api/admin/media/[id]` - получить signed URL
- `DELETE /api/admin/media/[id]` - удалить файл

### Аналитика
- `GET /api/admin/stats` - базовая статистика
- `GET /api/admin/analytics` - расширенная аналитика
- `GET /api/admin/audit-logs` - история действий

---

## ✨ Особенности реализации

1. **Транзакции** - все критические операции используют транзакции для атомарности
2. **Audit Logging** - все действия логируются
3. **Валидация** - все входные данные валидируются через Zod
4. **Безопасность** - проверка прав доступа на каждом endpoint
5. **Каскадное удаление** - правильная обработка зависимостей при удалении
6. **Пагинация** - поддержка limit/offset для больших списков
7. **Фильтрация** - гибкие фильтры для поиска данных

---

## 🎯 Итог

Администратор может управлять:
- ✅ Всеми пользователями (CRUD, сессии, 2FA, прогресс)
- ✅ Всеми курсами, модулями и уроками (CRUD, настройки, медиа)
- ✅ Всеми домашними заданиями (просмотр, удаление, история)
- ✅ Всеми комментариями (модерация, удаление)
- ✅ Всеми квизами (просмотр попыток, сброс)
- ✅ Уведомлениями (массовая отправка)
- ✅ Группами (CRUD, участники, массовое зачисление)
- ✅ Медиа-библиотекой (CRUD, signed URLs)
- ✅ Аналитикой и статистикой
- ✅ Audit logs (просмотр истории действий)
- ✅ Impersonation (вход от имени пользователя)

**Полный контроль над системой!** 🎉

