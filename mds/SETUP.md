# Инструкция по настройке проекта

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

**Обязательные переменные:**
- `DATABASE_URL` - URL подключения к PostgreSQL
- `JWT_SECRET` - Секретный ключ для JWT (сгенерируйте случайную строку)
- `JWT_REFRESH_SECRET` - Секретный ключ для refresh токенов
- `CLOUDFLARE_STREAM_API_TOKEN` - API токен Cloudflare Stream
- `CLOUDFLARE_ACCOUNT_ID` - ID аккаунта Cloudflare

### 3. Запуск Docker контейнеров

```bash
npm run docker:up
```

Или напрямую через docker-compose:

```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL на порту 5432
- Redis на порту 6379
- Next.js приложение на порту 3000
- Nginx на порту 80 (проксирует запросы к приложению)

**Важно:** Убедитесь, что в `.env` файле установлены все необходимые переменные:
- `JWT_SECRET` и `JWT_REFRESH_SECRET` (обязательно!)
- `DATABASE_URL` (будет использован из docker-compose, но можно переопределить)
- `REDIS_URL` (будет использован из docker-compose, но можно переопределить)

### 4. Настройка базы данных

Если вы запускаете через Docker Compose, миграции можно применить внутри контейнера:

```bash
# Применение миграций
docker-compose exec app npx prisma migrate deploy

# Заполнение тестовыми данными
docker-compose exec app npm run db:seed
```

Или локально (если запускаете dev сервер отдельно):

```bash
# Генерация Prisma Client
npm run db:generate

# Применение миграций
npm run db:migrate

# Заполнение тестовыми данными
npm run db:seed
```

**Тестовые пользователи:**
- Админ: `admin@proryv.ru` / `admin123`
- Студент: `student@test.ru` / `student123`
- Куратор: `curator@test.ru` / `curator123`

### 5. Доступ к приложению

**При запуске через Docker Compose:**
- Приложение доступно через Nginx: [http://localhost](http://localhost) (порт 80)
- Прямой доступ к приложению: [http://localhost:3000](http://localhost:3000)

**При локальной разработке:**
```bash
npm run dev
```
Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## 📋 Полезные команды

### Разработка
```bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка для продакшена
npm run start        # Запуск production сервера
npm run lint         # Проверка кода
npm run format       # Форматирование кода
npm run type-check   # Проверка TypeScript типов
```

### База данных
```bash
npm run db:generate  # Генерация Prisma Client
npm run db:push      # Применение изменений схемы
npm run db:migrate   # Создание/применение миграций
npm run db:studio    # Открыть Prisma Studio
npm run db:seed      # Заполнить БД тестовыми данными
```

### Docker
```bash
npm run docker:up    # Запуск контейнеров
npm run docker:down  # Остановка контейнеров
npm run docker:logs  # Просмотр логов
```

### Тестирование
```bash
npm run test         # Unit тесты
npm run test:ui      # Unit тесты с UI
npm run test:e2e     # E2E тесты
```

## 🔧 Настройка Cloudflare Stream

1. Создайте аккаунт в Cloudflare Stream
2. Получите API токен в настройках аккаунта
3. Добавьте токен в `.env`:
   ```
   CLOUDFLARE_STREAM_API_TOKEN=your-token-here
   CLOUDFLARE_ACCOUNT_ID=your-account-id
   ```
4. Настройте webhook в Cloudflare Stream dashboard:
   - URL: `https://yourdomain.com/api/webhooks/cloudflare-stream`
   - Secret: добавьте в `CLOUDFLARE_STREAM_WEBHOOK_SECRET`

## 🐛 Решение проблем

### Ошибка подключения к БД
- Убедитесь, что Docker контейнеры запущены: `npm run docker:up`
- Проверьте `DATABASE_URL` в `.env`
- Проверьте логи: `npm run docker:logs`

### Ошибки TypeScript
- Убедитесь, что Prisma Client сгенерирован: `npm run db:generate`
- Перезапустите TypeScript сервер в IDE

### Проблемы с миграциями
- Если миграции не применяются, попробуйте: `npm run db:push`
- Для сброса БД: удалите volume в Docker и пересоздайте

## 📚 Дополнительная документация

- [Архитектура проекта](./ARCHITECTURE.md)
- [Техническое задание](./TZ_FINAL.md)
- [README](./README.md)

