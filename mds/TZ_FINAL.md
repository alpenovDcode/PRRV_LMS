# Техническое задание: LMS-платформа Proryv.ru

**Цель проекта:** Создать масштабируемую LMS-платформу с фокусом на UX (уровень Linear/Netflix), высокой производительностью и гибкой системой доступов. Ключевое отличие: Отказ от монолитной архитектуры в пользу микросервисов/serverless и современного стека.

---

## 🛠 Технологический стек

### Frontend
- **Framework:** Next.js 14+ (App Router), TypeScript
- **UI Framework:** Tailwind CSS + Shadcn/UI (обязательно для скорости и единого стиля)
- **State Management:** 
  - React Query (TanStack Query) — для серверного состояния и кеширования
  - Zustand — для клиентского UI-состояния (модалки, тема, локальные формы)
- **Forms:** React Hook Form + Zod (валидация)
- **HTTP Client:** Axios или Fetch API с оберткой

### Backend
- **API:** Next.js API Routes (App Router) или отдельный Express/Fastify сервис
- **Database:** PostgreSQL (в Docker контейнере)
- **ORM:** Prisma или Drizzle ORM (рекомендуется Prisma для скорости разработки)
- **Migrations:** Prisma Migrate или Flyway
- **Connection Pooling:** PgBouncer (опционально, для продакшена)

### Видео и файлы
- **Видео хранение и стриминг:** Cloudflare Stream (безопасное хранение, автоматическая конвертация, адаптивный стриминг)
- **Видеоплеер:** Cloudflare Stream Player (встроенный) или кастомный плеер с Cloudflare Stream API
- **Загрузка файлов:** 
  - Для видео: Прямая загрузка через Cloudflare Stream API (поддержка больших файлов)
  - Для ДЗ и других файлов: Локальный сервер или S3-совместимое хранилище
- **Обработка видео:** Cloudflare Stream автоматически обрабатывает (конвертация, превью, метаданные)

### Инфраструктура и DevOps
- **Контейнеризация:** Docker + Docker Compose
- **Reverse Proxy:** Nginx (для статики и проксирования)
- **Мониторинг:** 
  - Sentry — для отслеживания ошибок
  - Vercel Analytics или Plausible — для аналитики производительности
- **Логирование:** Winston или Pino (структурированные логи)
- **CI/CD:** GitHub Actions или GitLab CI

### Тестирование
- **Unit Tests:** Vitest или Jest
- **E2E Tests:** Playwright
- **API Tests:** Supertest

### Дополнительно
- **Email:** Resend или SendGrid (для уведомлений)
- **CDN:** Cloudflare CDN (для статики и изображений)
- **Кеширование:** Redis (опционально, для сессий и кеша)
- **Cloudflare Stream:** 
  - API ключи для интеграции
  - Webhook endpoint для обработки событий (готовность видео, ошибки)
  - Настройка watermark templates

---

## 📅 ЭТАП 1: Фундамент и Архитектура БД (2-3 недели)

**Задача:** Развернуть проект, настроить БД, авторизацию и базовый UI.

### 1.1. Инфраструктура и окружение

#### 1.1.1. Docker Compose Setup
- Создать `docker-compose.yml` с сервисами:
  - PostgreSQL (с volume для персистентности)
  - Redis (опционально, для кеша)
  - Nginx (для статики и проксирования)
- Настроить переменные окружения через `.env` файлы:
  - `CLOUDFLARE_STREAM_API_TOKEN` — API токен для Cloudflare Stream
  - `CLOUDFLARE_ACCOUNT_ID` — ID аккаунта Cloudflare
  - `CLOUDFLARE_STREAM_WEBHOOK_SECRET` — секрет для проверки webhook'ов
- Настроить health checks для всех сервисов

#### 1.1.2. База данных (Schema Design)

Реализовать следующие таблицы в PostgreSQL:

**users**
```sql
- id: uuid (PRIMARY KEY, DEFAULT gen_random_uuid())
- email: varchar(255) (UNIQUE, NOT NULL)
- password_hash: varchar(255) (NOT NULL)
- full_name: varchar(255)
- avatar_url: varchar(500)
- role: enum('student', 'admin', 'curator') (DEFAULT 'student')
- session_id: varchar(255) (для контроля сессий)
- email_verified: boolean (DEFAULT false)
- created_at: timestamp (DEFAULT now())
- updated_at: timestamp (DEFAULT now())
```

**courses**
```sql
- id: uuid (PRIMARY KEY)
- title: varchar(255) (NOT NULL)
- slug: varchar(255) (UNIQUE, NOT NULL) -- url-friendly
- description: text
- cover_image: varchar(500)
- is_published: boolean (DEFAULT false)
- author_id: uuid (FOREIGN KEY -> users.id)
- created_at: timestamp
- updated_at: timestamp
```

**modules**
```sql
- id: uuid (PRIMARY KEY)
- course_id: uuid (FOREIGN KEY -> courses.id, ON DELETE CASCADE)
- title: varchar(255) (NOT NULL)
- order_index: integer (NOT NULL)
- created_at: timestamp
- updated_at: timestamp
```

**lessons**
```sql
- id: uuid (PRIMARY KEY)
- module_id: uuid (FOREIGN KEY -> modules.id, ON DELETE CASCADE)
- title: varchar(255) (NOT NULL)
- type: enum('video', 'text', 'quiz') (NOT NULL)
- content: jsonb -- для Markdown текста, quiz вопросов и т.д.
- video_id: varchar(255) -- Cloudflare Stream Video ID (NULLABLE, только для type='video')
- video_duration: integer -- длительность в секундах (синхронизируется из Cloudflare Stream)
- thumbnail_url: varchar(500) -- URL превью из Cloudflare Stream
- is_free: boolean (DEFAULT false) -- для промо-уроков
- is_stop_lesson: boolean (DEFAULT false) -- блокирует доступ к следующим урокам
- drip_rule: jsonb -- { "type": "after_start", "days": 3 }
- settings: jsonb -- дополнительные настройки
- order_index: integer (NOT NULL)
- created_at: timestamp
- updated_at: timestamp
```

**enrollments** (Доступы)
```sql
- id: uuid (PRIMARY KEY)
- user_id: uuid (FOREIGN KEY -> users.id, ON DELETE CASCADE)
- course_id: uuid (FOREIGN KEY -> courses.id, ON DELETE CASCADE)
- status: enum('active', 'expired', 'frozen') (DEFAULT 'active')
- start_date: date (NOT NULL) -- anchor date для дрип-контента
- expires_at: timestamp (NULLABLE) -- если NULL, доступ бессрочный
- created_at: timestamp
- updated_at: timestamp
- UNIQUE(user_id, course_id) -- один пользователь может иметь только одну запись на курс
```

**lesson_progress**
```sql
- user_id: uuid (FOREIGN KEY -> users.id, ON DELETE CASCADE)
- lesson_id: uuid (FOREIGN KEY -> lessons.id, ON DELETE CASCADE)
- status: enum('not_started', 'in_progress', 'completed') (DEFAULT 'not_started')
- watched_time: integer (DEFAULT 0) -- секунды просмотра для resume playback
- completed_at: timestamp (NULLABLE)
- last_updated: timestamp (DEFAULT now())
- PRIMARY KEY (user_id, lesson_id)
```

**homework_submissions**
```sql
- id: uuid (PRIMARY KEY)
- user_id: uuid (FOREIGN KEY -> users.id, ON DELETE CASCADE)
- lesson_id: uuid (FOREIGN KEY -> lessons.id, ON DELETE CASCADE)
- content: text -- текст ответа
- files: jsonb -- массив путей к файлам: ["/uploads/homework/123/file1.pdf"]
- status: enum('pending', 'approved', 'rejected') (DEFAULT 'pending')
- curator_comment: text
- curator_id: uuid (FOREIGN KEY -> users.id, NULLABLE)
- reviewed_at: timestamp (NULLABLE)
- created_at: timestamp
- updated_at: timestamp
```

**notifications**
```sql
- id: uuid (PRIMARY KEY)
- user_id: uuid (FOREIGN KEY -> users.id, ON DELETE CASCADE)
- type: varchar(50) -- 'enrollment', 'homework_approved', 'homework_rejected', etc.
- title: varchar(255)
- message: text
- is_read: boolean (DEFAULT false)
- link: varchar(500) -- ссылка на связанный объект
- created_at: timestamp
```

**Индексы:**
- `users.email` (UNIQUE)
- `courses.slug` (UNIQUE)
- `enrollments(user_id, course_id)` (UNIQUE)
- `lesson_progress(user_id, lesson_id)` (PRIMARY KEY)
- `enrollments.user_id`, `enrollments.course_id`
- `lessons.module_id`
- `modules.course_id`
- `notifications.user_id`, `notifications.is_read`

### 1.2. Авторизация и безопасность

#### 1.2.1. Реализация Auth
- **Вход через Email + Password:**
  - Хеширование паролей: bcrypt (с cost factor 10-12)
  - JWT токены для сессий (access token + refresh token)
  - Access token: короткоживущий (15-30 минут)
  - Refresh token: долгоживущий (7-30 дней), хранится в httpOnly cookie
  - Middleware для проверки токенов

- **Вход через Google (OAuth 2.0):**
  - Интеграция Google OAuth
  - Создание пользователя при первом входе
  - Связывание аккаунтов (если email совпадает)

#### 1.2.2. Контроль сессий
- При логине генерировать уникальный `session_id` и сохранять в БД
- Сохранять `session_id` в JWT payload
- Middleware проверяет: если `session_id` в токене не совпадает с БД -> разлогин
- При смене пароля или "Выйти везде" — инвалидировать все сессии пользователя

#### 1.2.3. Защита роутов
- **Middleware в Next.js:**
  - Проверка JWT токена
  - Редирект на `/login`, если нет авторизации
  - Проверка роли для админских роутов (`/admin/*`)
  - Возврат 403, если роль недостаточна

#### 1.2.4. Row Level Security (RLS) на уровне приложения
- **Студент:** Может читать только свои `enrollments`, свой `lesson_progress`, свои `homework_submissions`
- **Куратор:** Может читать все `homework_submissions` в назначенных ему курсах
- **Админ:** Полный доступ ко всем данным
- Реализовать через middleware и проверки в API routes

### 1.3. UI Лейаут и дизайн-система

#### 1.3.1. AppShell
- Боковое меню (Sidebar):
  - Свернутое/развернутое состояние
  - Навигация по разделам
  - Адаптивное (скрывается на мобильных)
- Хедер (Header):
  - Логотип
  - Поиск (опционально)
  - Уведомления (колокольчик с индикатором)
  - Профиль пользователя (dropdown)
- Основной контент (Main Content Area)

#### 1.3.2. Темная/Светлая тема
- Использовать `next-themes` для переключения
- Сохранение предпочтения в localStorage
- Поддержка системной темы (prefers-color-scheme)
- Кастомные CSS переменные для цветов в Tailwind

#### 1.3.3. Компоненты (Shadcn/UI)
- Базовая библиотека: Button, Input, Card, Dialog, Dropdown, Toast
- Кастомные компоненты:
  - CourseCard
  - LessonItem
  - ProgressBar
  - VideoPlayer (обертка над Cloudflare Stream Player)

---

## 📅 ЭТАП 2: MVP — Обучение и Базовый функционал (4-5 недель)

**Задача:** Чтобы студент мог зайти, получить доступ к курсу и посмотреть урок.

### 2.1. Конструктор Уроков (Admin Side)

#### 2.1.1. CRUD Курсов
- **Создание курса:**
  - Форма: название, описание, slug (автогенерация), обложка
  - Загрузка обложки (drag-and-drop или file input)
  - Превью перед сохранением
- **Редактирование курса:**
  - Те же поля + возможность изменить slug (с проверкой уникальности)
- **Удаление курса:**
  - Подтверждение через диалог
  - Каскадное удаление модулей и уроков (или мягкое удаление)

#### 2.1.2. CRUD Модулей
- Создание модуля внутри курса
- Редактирование названия
- Удаление (с предупреждением о каскадном удалении уроков)

#### 2.1.3. CRUD Уроков
- **Создание урока:**
  - Выбор типа (video/text/quiz)
  - Название, описание
  - Для видео: загрузка файла
  - Для текста: Markdown редактор (например, react-markdown-editor-lite)
  - Для quiz: конструктор вопросов (JSON структура)
  - Настройки: `is_free`, `is_stop_lesson`, `drip_rule`
- **Редактирование урока**
- **Удаление урока**

#### 2.1.4. Drag-and-Drop сортировка
- Использовать библиотеку `@dnd-kit/core` или `react-beautiful-dnd`
- Сортировка модулей внутри курса
- Сортировка уроков внутри модуля
- Сохранение `order_index` в БД после перетаскивания
- Оптимистичное обновление UI

#### 2.1.5. Загрузка Видео через Cloudflare Stream
- **Интеграция Cloudflare Stream API:**
  - Backend endpoint: `POST /api/lessons/[id]/upload-video`
  - Получение одноразового upload URL от Cloudflare Stream API
  - Возврат upload URL клиенту для прямой загрузки
- **Процесс загрузки:**
  1. Админ выбирает видеофайл в форме
  2. Фронтенд отправляет запрос на backend для получения upload URL
  3. Backend создает запись в Cloudflare Stream через API и получает upload URL
  4. Фронтенд загружает файл напрямую в Cloudflare Stream (прогресс-бар)
  5. Cloudflare Stream автоматически обрабатывает видео (конвертация, превью)
  6. Backend получает webhook от Cloudflare Stream о завершении обработки
  7. Сохранение `video_id` и метаданных (длительность, thumbnail) в БД
- **Webhook обработка:**
  - Endpoint: `POST /api/webhooks/cloudflare-stream`
  - Проверка подписи webhook (безопасность)
  - Обновление записи урока с `video_id` и метаданными
  - Уведомление админа о готовности видео
- **Преимущества:**
  - Автоматическая конвертация в адаптивные форматы
  - Генерация превью
  - Безопасное хранение
  - Глобальный CDN для быстрой доставки

### 2.2. Плеер Урока (Student Side)

#### 2.2.1. Лейаут страницы урока
- **Левая панель:** Меню курса (аккордеон)
  - Список модулей
  - Внутри каждого модуля — список уроков
  - Визуальная индикация: пройденные (галочка), текущий (подсветка), недоступные (серые)
  - Клик по уроку — переход на него
- **Правая панель:** Контент урока
  - Для видео: видеоплеер
  - Для текста: Markdown рендеринг
  - Для quiz: интерактивная форма

#### 2.2.2. Видеоплеер Cloudflare Stream
- **Использование Cloudflare Stream Player:**
  - Встроенный плеер от Cloudflare (Stream Player) или кастомная обертка
  - Поддержка адаптивного стриминга (автоматический выбор качества)
- **Функционал:**
  - Play/Pause
  - Прогресс-бар
  - Громкость
  - Полноэкранный режим
  - Скорость воспроизведения (0.5x, 1x, 1.5x, 2x)
  - Автоматическое качество (адаптивный стриминг)
  - Resume playback (возобновление с сохраненной позиции)
- **Получение токена для просмотра:**
  - Backend endpoint: `GET /api/lessons/[id]/stream-token`
  - Проверка доступа пользователя
  - Генерация signed URL или токена для Cloudflare Stream
  - Токен с ограниченным временем жизни (например, 1 час)
- **Автосохранение прогресса:**
  - Отслеживание времени просмотра через события плеера
  - Отправлять на сервер каждые 10-15 секунд `watched_time`
  - Debounce запросов
- **Автоматическая пометка "Пройден":**
  - При просмотре 90% видео -> обновить `status = 'completed'` в `lesson_progress`
  - Визуальная индикация (галочка в меню)

#### 2.2.3. Навигация между уроками
- Кнопка "Предыдущий урок" (если есть)
- Кнопка "Следующий урок" (если доступен)
- Автоматический переход на следующий урок после завершения (опционально)

#### 2.2.4. Проверка доступа
- Middleware перед загрузкой урока:
  - Проверить наличие активного `enrollment`
  - Проверить `expires_at` (если задан)
  - Проверить дрип-контент (если применимо)
  - Проверить стоп-уроки (если применимо)
  - Вернуть 403 с понятным сообщением, если доступ закрыт

### 2.3. Система доступов (Enrollments)

#### 2.3.1. Выдача доступа вручную (Admin)
- Страница `/admin/courses/[id]/enrollments`
- Форма: выбор пользователя (autocomplete) + дата начала + срок действия
- Создание записи в `enrollments`

#### 2.3.2. API для проверки доступа
- Endpoint: `GET /api/courses/[id]/access`
- Возвращает: `{ hasAccess: boolean, enrollment: {...} | null }`

---

## 📅 ЭТАП 3: CRM и Домашние задания (3-4 недели)

**Задача:** Обеспечить обратную связь и управление базой.

### 3.1. Система Домашних Заданий (Homework)

#### 3.1.1. Student UI: Форма отправки ДЗ
- Расположение: под видеоплеером (или в отдельной вкладке)
- Поля:
  - Текстовое поле (textarea с поддержкой Markdown)
  - Drag-and-drop зона для файлов
  - Максимальный размер файла: 50MB (настраиваемо)
  - Поддержка множественных файлов
  - Превью загруженных файлов
- Кнопка "Отправить на проверку"
- Валидация: минимум 10 символов текста или хотя бы один файл

#### 3.1.2. Backend: Обработка загрузки
- Endpoint: `POST /api/lessons/[id]/homework`
- Сохранение файлов в `/uploads/homework/{user_id}/{lesson_id}/`
- Создание записи в `homework_submissions`
- Возврат успешного ответа

#### 3.1.3. Curator UI: Лента входящих ответов
- Страница `/admin/homework` или `/curator/homework`
- Фильтры:
  - По статусу (pending/approved/rejected)
  - По курсу
  - По дате
- Список карточек:
  - Информация о студенте
  - Название урока
  - Текст ответа
  - Список файлов (с возможностью скачать)
  - Статус
- **Действия:**
  - Просмотр полного ответа (в модалке или на отдельной странице)
  - Поле для комментария куратора
  - Кнопки "Принять" и "Отклонить"
  - При смене статуса -> обновление БД + создание уведомления

#### 3.1.4. Student UI: История отправок
- Страница `/my/homework` или вкладка в профиле
- Список всех отправленных ДЗ с статусами
- Возможность просмотреть комментарий куратора

### 3.2. Логика "Стоп-уроков"

#### 3.2.1. Middleware/Guard
- При запросе урока (API route):
  1. Проверить базовый доступ (enrollment)
  2. Если урок имеет `is_stop_lesson = true`:
     - Найти предыдущий урок (по `order_index`)
     - Проверить, есть ли у предыдущего урока `is_stop_lesson = true`
     - Если да -> проверить статус ДЗ в `homework_submissions`
     - Если `status != 'approved'` -> вернуть 403 с сообщением: "Доступ закрыт. Сдайте домашнее задание к предыдущему уроку."
  3. Если все проверки пройдены -> вернуть данные урока

#### 3.2.2. UI индикация
- В меню курса: недоступные уроки серые, с иконкой замка
- При клике на недоступный урок -> показать toast с объяснением

### 3.3. Карточка Клиента (CRM Light)

#### 3.3.1. Страница `/admin/users/[id]`
- **Информация о пользователе:**
  - Email, имя, роль
  - Дата регистрации
  - Статус email (подтвержден/нет)
  - Последний вход
- **Список покупок/доступов:**
  - Таблица с курсами
  - Статус доступа (active/expired/frozen)
  - Дата начала и окончания
  - Кнопка "Продлить доступ" (открывает модалку с выбором даты)
- **Прогресс обучения:**
  - По каждому курсу: процент пройденных уроков
  - Список последних активностей
- **Домашние задания:**
  - Статистика: отправлено/принято/отклонено
  - Ссылка на список всех ДЗ пользователя

#### 3.3.2. Действия администратора
- **Продлить доступ:**
  - Модалка: выбор новой даты `expires_at`
  - Обновление записи в `enrollments`
- **Заморозить/Разморозить доступ:**
  - Изменение `status` в `enrollments`
- **Зайти под пользователем (Impersonation):**
  - Генерация временного JWT токена с флагом `impersonated_by: admin_id`
  - Сохранение в сессии админа информации об impersonation
  - Баннер в UI: "Вы вошли как {user_email}. [Выйти]"
  - Все действия логируются

---

## 📅 ЭТАП 4: Advanced Features (5-6 недель)

**Задача:** Реализовать сложную логику для удержания пользователей.

### 4.1. Дрип-контент (Расписание)

#### 4.1.1. Структура `drip_rule` в БД
```json
{
  "type": "after_start", // или "on_date"
  "days": 3, // для after_start
  "date": "2024-10-25" // для on_date
}
```

#### 4.1.2. Логика расчета доступности
- **API Endpoint:** `GET /api/courses/[id]/lessons`
- Алгоритм:
  1. Получить `enrollment.start_date` для пользователя
  2. Для каждого урока проверить `drip_rule`:
     - Если `type = "after_start"`: `available_date = start_date + days`
     - Если `type = "on_date"`: `available_date = date`
     - Если `drip_rule` отсутствует -> урок доступен сразу
  3. Сравнить `available_date` с текущей датой (с учетом таймзоны пользователя)
  4. Вернуть список уроков с флагом `is_available` и `available_date`

#### 4.1.3. UI отображение
- В меню курса:
  - Доступные уроки — обычный стиль
  - Недоступные уроки — серые, с иконкой часов
  - Тултип: "Откроется 25 октября 2024"
- На странице недоступного урока:
  - Блокировка контента
  - Сообщение: "Этот урок станет доступен 25 октября 2024"

#### 4.1.4. Уведомления о открытии
- Cron job (или scheduled task):
  - Ежедневно проверять уроки, которые должны открыться сегодня
  - Создавать уведомления для пользователей

### 4.2. Защита Контента

#### 4.2.1. Контроль сессий (уже описано в 1.2.2)
- Дополнительно: логирование подозрительной активности
  - Множественные сессии с разных IP
  - Быстрая смена IP адресов

#### 4.2.2. Watermark на видео через Cloudflare Stream
- **Использование встроенных возможностей Cloudflare Stream:**
  - Cloudflare Stream поддерживает наложение watermark через API
  - Создание watermark template в Cloudflare Stream (один раз)
  - Привязка watermark к видео при генерации токена просмотра
- **Генерация токена с watermark:**
  - Backend endpoint: `GET /api/lessons/[id]/stream-token`
  - При запросе токена указывать параметры watermark:
    - Текст: Email пользователя или user_id
    - Позиция: нижний правый угол
    - Прозрачность: настраиваемая
  - Cloudflare Stream автоматически накладывает watermark при стриминге
- **Защита от скачивания:**
  - Cloudflare Stream не отдает прямые ссылки на файлы
  - Использование signed URLs с ограниченным временем жизни
  - Токены привязаны к конкретному пользователю
  - CORS политики настроены в Cloudflare Stream
  - Дополнительно: отключение правой кнопки мыши и DevTools detection (на фронтенде)

#### 4.2.3. Защита текстового контента
- Отключить копирование текста (CSS: `user-select: none`)
- Предупреждение при попытке скриншота (через DevTools detection, но это не 100% защита)
- Watermark на скриншотах (через CSS background или canvas overlay)

### 4.3. Уведомления

#### 4.3.1. Таблица `notifications` (уже описана в 1.1.2)

#### 4.3.2. Триггеры и события
- **При создании enrollment:**
  - Создать уведомление типа `enrollment` с сообщением "Вам открыт доступ к курсу {course_title}"
- **При смене статуса ДЗ:**
  - Создать уведомление `homework_approved` или `homework_rejected`
- **При открытии дрип-урока:**
  - Создать уведомление `lesson_available`
- Реализация: Database triggers (PostgreSQL) или application-level hooks (в Prisma/ORM)

#### 4.3.3. UI: Колокольчик
- Компонент в хедере
- Красный индикатор с количеством непрочитанных
- Dropdown при клике:
  - Список последних 10-20 уведомлений
  - Маркировка как прочитанное при клике
  - Ссылка на связанный объект (урок, ДЗ и т.д.)
- Страница `/notifications` для полного списка

#### 4.3.4. Real-time обновления (опционально)
- WebSocket или Server-Sent Events для мгновенных уведомлений
- Или polling каждые 30 секунд (проще, но менее эффективно)

### 4.4. Дополнительные фичи

#### 4.4.1. Поиск по курсам и урокам
- Full-text search в PostgreSQL (tsvector/tsquery)
- Или интеграция Elasticsearch (для больших объемов)
- UI: поисковая строка в хедере

#### 4.4.2. Избранное/Закладки
- Таблица `bookmarks` (user_id, lesson_id)
- Кнопка "В избранное" на странице урока

#### 4.4.3. Комментарии к урокам
- Таблица `comments` (user_id, lesson_id, content, parent_id для ответов)
- UI: секция комментариев под уроком

---

## 🔒 Нефункциональные требования (Quality Assurance)

### Производительность

#### Frontend
- **Google Lighthouse Score > 90:**
  - Использовать `next/image` для оптимизации картинок
  - Lazy loading для компонентов (React.lazy)
  - Code splitting по роутам
  - Минификация и сжатие (gzip/brotli)
  - CDN для статики

#### Backend
- **Оптимизация запросов:**
  - Использовать индексы БД
  - Избегать N+1 запросов (использовать JOIN или Prisma include)
  - Кеширование частых запросов (Redis)
- **Пагинация:**
  - Все списки с пагинацией (limit/offset или cursor-based)
- **Асинхронная обработка:**
  - Тяжелые задачи (отправка email, обработка webhook'ов) в фоновые jobs (Bull/BullMQ или простая очередь)
  - Конвертация видео обрабатывается автоматически Cloudflare Stream (не требует серверных ресурсов)

### Безопасность

#### 1. Row Level Security (на уровне приложения)
- Все API routes проверяют права доступа
- Студент не может получить доступ к чужим данным
- Админ имеет полный доступ

#### 2. Валидация входных данных
- Zod схемы для всех форм
- Sanitization пользовательского контента (Markdown, HTML)
- Защита от SQL injection (ORM автоматически экранирует)
- Защита от XSS (React автоматически экранирует, но проверять пользовательский контент)

#### 3. Rate Limiting
- Ограничение количества запросов с одного IP
- Особенно для endpoints авторизации и загрузки файлов
- Использовать `express-rate-limit` или аналоги

#### 4. CORS
- Настроить CORS политики для API
- Разрешать только доверенные домены

#### 5. HTTPS
- Обязательно в продакшене
- Настроить через Nginx или reverse proxy

### Обработка ошибок

#### Frontend
- **Toast notifications:**
  - Использовать библиотеку `sonner` или `react-hot-toast`
  - Понятные сообщения: "Не удалось загрузить файл. Попробуйте еще раз."
  - Автоматическое скрытие через 5 секунд
- **Error boundaries:**
  - React Error Boundaries для ловли ошибок рендеринга
  - Fallback UI с кнопкой "Перезагрузить страницу"

#### Backend
- **Структурированные ошибки:**
  - Единый формат ответа: `{ error: { code, message, details } }`
  - HTTP статус коды: 400 (валидация), 401 (не авторизован), 403 (нет доступа), 404 (не найдено), 500 (серверная ошибка)
- **Логирование:**
  - Все ошибки логировать с контекстом (user_id, IP, request body)
  - Не логировать чувствительные данные (пароли, токены)

### Код-стайл и качество

#### 1. ESLint + Prettier
- Конфигурация для TypeScript/React
- Pre-commit hooks (husky + lint-staged)

#### 2. TypeScript
- Строгий режим (`strict: true`)
- Избегать `any`, использовать типы везде

#### 3. Компонентная архитектура
- **Atomic Design:**
  - Atoms: Button, Input, Card
  - Molecules: FormField, CourseCard
  - Organisms: CourseList, LessonPlayer
  - Templates: AppShell, AuthLayout
- Переиспользуемые компоненты
- Документация компонентов (Storybook опционально)

#### 4. Тестирование
- **Unit тесты:**
  - Критичные функции (расчет дрип-контента, проверка доступа)
  - Утилиты и хелперы
- **E2E тесты:**
  - Критичные сценарии: регистрация, вход, просмотр урока, отправка ДЗ
- Цель покрытия: минимум 60% для критичных модулей

### Мониторинг и аналитика

#### 1. Отслеживание ошибок
- Sentry для production ошибок
- Алерты при критичных ошибках

#### 2. Аналитика производительности
- Vercel Analytics или Plausible
- Отслеживание метрик: время загрузки страниц, API response time

#### 3. Логирование
- Структурированные логи (JSON формат)
- Ротация логов (чтобы не забивать диск)
- Централизованное хранение (опционально: ELK stack)

---

## 📊 Оценка сроков (реалистичная)

| Этап | Оптимистично | Реалистично | С запасом |
|------|--------------|-------------|-----------|
| Этап 1: Фундамент | 2 недели | 3 недели | 4 недели |
| Этап 2: MVP | 4 недели | 5 недель | 6 недель |
| Этап 3: CRM и ДЗ | 3 недели | 4 недели | 5 недель |
| Этап 4: Advanced | 5 недель | 6 недель | 8 недель |
| Тестирование и багфиксы | - | 2 недели | 3 недели |
| **ИТОГО** | **14 недель** | **20 недель** | **26 недель** |

**Рекомендация:** Планировать на **20 недель (5 месяцев)** при команде из 2-3 разработчиков.

---

## 🚀 Деплой и инфраструктура

### Development
- Docker Compose для локальной разработки
- Hot reload для фронтенда и бэкенда
- Отдельная БД для разработки

### Staging
- Отдельный сервер/контейнер для тестирования
- Автоматический деплой при merge в `develop` ветку

### Production
- **Варианты хостинга:**
  - VPS (Hetzner, DigitalOcean) с Docker
  - AWS/GCP с контейнерами
  - Vercel (для фронтенда) + отдельный сервер для API и БД
- **Бэкапы:**
  - Ежедневные бэкапы БД (pg_dump)
  - Хранение бэкапов на отдельном сервере/S3
  - Тестирование восстановления раз в месяц
- **Масштабирование:**
  - Горизонтальное масштабирование API (несколько инстансов)
  - Load balancer (Nginx или cloud load balancer)
  - Видео обрабатывается и доставляется через Cloudflare Stream (не требует серверных ресурсов)

---

## 📝 Чеклист перед запуском

- [ ] Все миграции БД применены
- [ ] Переменные окружения настроены (включая Cloudflare Stream API ключи)
- [ ] Cloudflare Stream аккаунт настроен:
  - [ ] API токен создан и добавлен в переменные окружения
  - [ ] Webhook endpoint настроен в Cloudflare Stream dashboard
  - [ ] Watermark templates созданы (если используется)
- [ ] SSL сертификат установлен (HTTPS)
- [ ] Бэкапы настроены
- [ ] Мониторинг и логирование работают
- [ ] Rate limiting включен
- [ ] Тесты пройдены
- [ ] Документация API написана (опционально: Swagger/OpenAPI)
- [ ] Инструкция по деплою для команды

---

**Версия документа:** 1.0  
**Дата:** 2024  
**Автор:** AI Assistant (на основе требований заказчика)

