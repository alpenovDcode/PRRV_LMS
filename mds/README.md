# Proryv.ru LMS

Масштабируемая LMS-платформа с фокусом на UX, высокой производительностью и гибкой системой доступов.

## 🛠 Технологический стек

- **Frontend:** Next.js 14+ (App Router), TypeScript, Tailwind CSS, Shadcn/UI
- **Backend:** Next.js API Routes, Prisma ORM
- **Database:** PostgreSQL
- **Видео:** Cloudflare Stream
- **Кеширование:** Redis
- **Контейнеризация:** Docker Compose

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+
- Docker и Docker Compose
- npm или yarn

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Proryv_ru_LMS
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл и добавьте необходимые ключи
```

4. Запустите Docker контейнеры:
```bash
npm run docker:up
```

5. Настройте базу данных:
```bash
npm run db:generate
npm run db:migrate
```

6. Запустите dev сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

## 📁 Структура проекта

```
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   ├── (auth)/            # Auth pages
│   ├── admin/             # Admin pages
│   └── ...
├── components/            # React компоненты
│   ├── ui/               # Shadcn/UI компоненты
│   └── ...
├── lib/                   # Утилиты и хелперы
│   ├── db.ts             # Prisma client
│   ├── auth.ts           # Auth утилиты
│   └── ...
├── prisma/                # Prisma schema и migrations
├── types/                 # TypeScript типы
└── public/                # Статические файлы
```

## 🗄 База данных

### Миграции

```bash
# Создать новую миграцию
npm run db:migrate

# Применить миграции
npm run db:push

# Открыть Prisma Studio
npm run db:studio
```

## 🧪 Тестирование

```bash
# Unit тесты
npm run test

# E2E тесты
npm run test:e2e
```

## 📝 Скрипты

- `npm run dev` - Запуск dev сервера
- `npm run build` - Сборка для продакшена
- `npm run start` - Запуск production сервера
- `npm run lint` - Проверка кода
- `npm run format` - Форматирование кода
- `npm run type-check` - Проверка TypeScript типов

## 🔒 Безопасность

- JWT токены для авторизации
- Bcrypt для хеширования паролей
- Row Level Security на уровне приложения
- Валидация через Zod
- Rate limiting

## 📚 Документация

Подробная документация находится в файле [TZ_FINAL.md](./TZ_FINAL.md)

## 📄 Лицензия

Private

